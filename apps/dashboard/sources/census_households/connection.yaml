name: census_households
type: sqlite
options:
  # 世帯の種類別 世帯数・世帯人員（全国・都道府県／1960〜2020）。市区町村は持たない（ソース非対称）。
  # 単一 ID 0003410420 に全国＋県粒度を含む最小構成。data/ は非コミットのためローカル出力前提。
  filename: ../../../../data/processed/census_households.sqlite
