name: census_age5year_municipality
type: duckdb
options:
  # サンプル市区町村（印西市）抽出用＝市区町村ミクロの年齢5歳階級×男女×国籍別 時系列（1980〜2020）。
  # ★ミクロ系列は 100歳以上（310）まで細粒度・国籍（総数/日本人）2軸を持つ＝マクロ県表より細かい。
  # read_parquet で精製 parquet を直接参照。全国・都道府県マクロは census_age5year_prefecture 側。
  filename: ':memory:'
